<Window x:Class="VideoRecorderScreen.Views.ConflictDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Conflict_Title}"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">
    <StackPanel Margin="20">
        <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="13" Margin="0,0,0,20"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnOverwrite" Content="{DynamicResource Conflict_Overwrite}"
                    Width="120" Margin="0,0,8,0" Click="OnOverwrite"/>
            <Button x:Name="BtnNumbered"  Content="{DynamicResource Conflict_SaveNumbered}"
                    Width="140" Margin="0,0,8,0" Click="OnNumbered"/>
            <Button x:Name="BtnCancel"    Content="{DynamicResource Btn_Cancel}"
                    Width="80" Click="OnCancel" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
